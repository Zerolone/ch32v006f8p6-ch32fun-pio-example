#include "ch32fun.h"
#include <stdio.h>

int main()
{
	SystemInit();

	// Enable GPIOs
	funGpioInitAll();
	
	funPinMode( PD4, MODE_OUTPUT );

	while(1)
	{
		funDigitalWrite( PD4, FUN_HIGH );
		Delay_Ms( 1550 );

		funDigitalWrite( PD4, FUN_LOW );
		Delay_Ms( 550 );

		printf( "Flash !\n" );
	}
}
